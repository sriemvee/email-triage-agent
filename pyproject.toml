[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"

[project]
name = "email-triage-agent"
version = "0.1.0"
description = "A lightweight review-first email triage starter."
readme = "README.md"
requires-python = ">=3.11"
dependencies = []

[project.scripts]
email-triage-agent = "email_triage_agent.cli:main"

[tool.setuptools]
package-dir = {"" = "src"}

[tool.setuptools.packages.find]
where = ["src"]

